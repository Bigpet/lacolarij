Tools: use `uv` for running any python code and package management.
use `npm run test:e2e` in the frontend directory to execute playwright.

Look at docs/llm_use/llm_readme.md and docs/project_state.md for context
