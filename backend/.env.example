# JiraLocal Backend Environment Variables
# Copy this file to .env and the start-server.sh script will auto-generate keys if missing

# Database Configuration
# DATABASE_TYPE=sqlite  # Options: sqlite, postgresql (default: sqlite)
# DATABASE_URL=         # Optional: Full database URL (overrides individual settings)

# PostgreSQL Settings (only used when DATABASE_TYPE=postgresql)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_USER=jiralocal
# POSTGRES_PASSWORD=
# POSTGRES_DATABASE=jiralocal

# SQLite Settings (only used when DATABASE_TYPE=sqlite, which is the default)
# DATABASE_PATH=backend/data/jiralocal.db

# JWT signing key (auto-generated if not set)
# SECRET_KEY=your_secret_key_here

# Fernet encryption key for JIRA API tokens (auto-generated if not set)
# ENCRYPTION_KEY=your_fernet_key_here

# Optional: Override default settings
# HOST=127.0.0.1
# PORT=8080
# DEBUG=false
