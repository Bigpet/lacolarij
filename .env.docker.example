# JiraLocal Docker Environment Variables
# Copy this file to .env.docker and fill in the values before running docker-compose

# ============================================
# OPTIONAL - Configuration
# ============================================

# Host port to expose the application on (default: 6555)
# For production on port 80, set: HOST_PORT=80
# HOST_PORT=6555

# ============================================
# REQUIRED - Must be set before deployment
# ============================================

# JWT signing key (generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))")
SECRET_KEY=

# Fernet encryption key for API tokens (generate with: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ENCRYPTION_KEY=

# ============================================
# PostgreSQL Only (for docker-compose.postgres.yml)
# ============================================

# PostgreSQL password (required for PostgreSQL setup)
POSTGRES_PASSWORD=

# PostgreSQL user (optional, default: jiralocal)
# POSTGRES_USER=jiralocal

# PostgreSQL database name (optional, default: jiralocal)
# POSTGRES_DATABASE=jiralocal
